# Dependencias: se reinstalan con `npm install`.
node_modules/

# Vercel CLI
.vercel/

# Basura del sistema
Thumbs.db
desktop.ini
.DS_Store

# Registros
*.log
npm-debug.log*

# NO se ignora styles/app.css: es el CSS ya compilado y la página lo necesita.
# Vercel no ejecuta build (package.json está en .vercelignore), así que ese
# archivo tiene que viajar en el repo.
